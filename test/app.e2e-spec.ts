/**
 * Full e2e tests require a running PostgreSQL instance and env vars.
 * Run manually when DATABASE_URL and JWT_SECRET are set.
 */
describe.skip('App e2e (manual)', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
